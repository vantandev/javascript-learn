/*
Khai báo một array "listAnimal" chứa các object mô tả danh sách các con vật.
Mỗi con vật có 2 thuộc tính: name và age trong đó name có kiểu string, age có kiểu number.
 * {name}: string;
 * {age}: number;
*/
var cat = {
    name: 'meo',
    age: 5
};

var mouse = {
    name: 'tommy',
    age: 6
};

var listAnimal = [ cat, mouse ];
console.log(cat);
console.log(mouse);